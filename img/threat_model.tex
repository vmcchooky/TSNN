\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=4cm,
        entity/.style={rectangle, draw, fill=blue!10, minimum width=2.5cm, minimum height=1.5cm, align=center, font=\small\bfseries, rounded corners},
        process/.style={circle, draw, fill=green!10, minimum size=2.5cm, align=center, font=\small\bfseries},
        datastore/.style={cylinder, draw, fill=orange!10, minimum width=2cm, minimum height=2cm, shape border rotate=90, aspect=0.25, align=center, font=\small\bfseries},
        flow/.style={->, thick, >=stealth}
    ]

    % Nodes
    \node[entity] (user) {Người dùng\\(Trình duyệt)};
    \node[process, right=of user, xshift=-0.5cm] (backend) {Máy chủ xử lý\\(Go Backend)};
    \node[datastore, right=of backend, xshift=-0.5cm] (redis) {Bộ nhớ đệm\\(Redis)};

    % Boundary box for Server
    \begin{pgfonlayer}{background}
        \node[rectangle, draw=red, dashed, thick, fill=red!5, inner sep=0.6cm, fit=(backend) (redis)] (server_boundary) {};
        \node[above, text=red, font=\bfseries, yshift=-0.2cm] at (server_boundary.north) {Môi trường Máy chủ (Không tin cậy / Tiềm ẩn rủi ro)};
        
        \node[rectangle, draw=green!60!black, dashed, thick, fill=green!5, inner sep=0.6cm, fit=(user)] (local_boundary) {};
        \node[above, text=green!60!black, font=\bfseries, yshift=-0.2cm] at (local_boundary.north) {Môi trường Cục bộ (Tin cậy)};
    \end{pgfonlayer}

    % Flows
    \draw[flow] (user.20) -- node[above, font=\scriptsize] {1. Gửi Ciphertext (Tạo thư)} (backend.160);
    \draw[flow] (backend.200) -- node[below, font=\scriptsize] {4. Trả về Ciphertext (Đọc thư)} (user.340);
    
    \draw[flow] (backend.20) -- node[above, font=\scriptsize] {2. Lưu trữ bản mã vô nghĩa} (redis.160);
    \draw[flow] (redis.200) -- node[below, font=\scriptsize] {3. Truy xuất \& Tự hủy (Burn)} (backend.340);

    \end{tikzpicture}
    \caption[Sơ đồ Luồng Dữ liệu (DFD) và Ranh giới Tin cậy]{Sơ đồ Luồng Dữ liệu (DFD) thể hiện Ranh giới Tin cậy (Trust Boundaries) trong kiến trúc}
    \label{fig:threat_model_dfd}
\end{figure}
